---
type: tool_used
name: loads-rust-skill
tool: Skill
input_match: code-review-rust
---

A Rust review request must load `code-review-rust`. Under the default
with/without ablation this grader is a plugin-fired indicator, not part of the
score — it is the signal that the skill still triggers.
