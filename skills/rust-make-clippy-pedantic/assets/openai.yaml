# Agent settings
model: gpt-4o
temperature: 0.2
max_tokens: 16384
display_name: Make Clippy Pedantic
short_description: Lint a clean Rust checkout at pedantic strength without modifying it, file one backlog task per warning (test-only style findings, generated files and out-of-tree warnings dropped, high-volume lints aggregated per crate), and estimate the cleanup effort.
default_prompt: "Run rust-make-clippy-pedantic. Verify the git tree is clean, take a default-level baseline with cargo clippy --workspace --all-targets --locked, re-run the same command with -W clippy::pedantic -W clippy::nursery -W clippy::cargo passed as flags after --, create one backlog task per finding labelled pedantic with --modified-file set — dropping test-only style findings, generated files and out-of-tree warnings, and aggregating any lint that fires more than twenty times in one crate into a single task — then report what ran, what was filed, and a high-level effort estimate."
