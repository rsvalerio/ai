# TRAIT rules

## Traits & Generics (typical severity: Low--Medium)

- **TRAIT-1.** `From`/`Into` for conversions
- **TRAIT-2.** `impl Trait` in return position for flexibility; in 2024 edition, all lifetimes are captured by default (use `+ use<>` to opt out)
- **TRAIT-3.** Complex bounds → `where` clauses
- **TRAIT-4.** Derive deliberately, not reflexively. `Debug` is usually safe and helpful (except on secret-bearing types — see SEC-5); `Clone`, `Copy`, `Hash`, `PartialEq`/`Eq` are semantic commitments and part of your public API — once derived, removing is a breaking change. Derive only when the type semantically supports the trait and callers need it. `clippy::missing_debug_implementations` partially covers `Debug`; this rule's unique value is the "derive with intent" philosophy (see OWN-8, PERF-3 for clone-usage guidance and SEC-5 for secret types).
  **`Default` is the derive this is most often forgotten for**, because it reads as a convenience rather than a claim. It is a claim: that the all-zeroes value is meaningful. `#[derive(Default)]` on `struct ServerConfig { port: u16, max_connections: usize, timeout: Duration }` produces port 0, zero permitted connections, and a zero timeout — three states the type should never hold, now reachable through `..Default::default()` (DUP-7), through `unwrap_or_default()`, and through every generic bound that requires `Default`. Decide explicitly: if no value is genuinely sensible, do not implement it, and make callers name the required parameters in a constructor; if some fields have defaults and others do not, the type wants a builder (API-4), not a `Default`. If a default *is* meaningful but is not the zero value, write the impl by hand (on an enum, `#[default]` on the intended variant does it). The durable fix is at the field level — `NonZero<u16>`, or a `Port` newtype per API-2 — because a field type with no `Default` makes the wrong derive fail to compile instead of producing a wrong value
- **TRAIT-5.** Blanket impls sparingly (coherence conflicts)
- **TRAIT-6.** Separate read/write traits (ISP)
- **TRAIT-7.** `dyn Trait` only when dynamic dispatch needed
- **TRAIT-8.** Prefer traits over macros (better errors, IDE support)
- **TRAIT-9.** Extract traits when needed for multiple implementations, stable public APIs, or test doubles; start concrete until then. For module boundary design, see ARCH-2
- **TRAIT-10.** Use Generic Associated Types (GATs, stable since Rust 1.65) when an associated type needs to borrow from `&self` or carry a lifetime — e.g., a streaming-iterator-style `trait LendingIterator { type Item<'a> where Self: 'a; }`. Do not adopt GATs pre-emptively; they are a powerful tool for lending-iterator and allocator-like APIs but carry steeper error messages and inference costs than plain associated types
- **TRAIT-11.** On traits whose bounds are commonly unsatisfied, attach `#[diagnostic::on_unimplemented(message = "…", note = "…", label = "…")]` (stable since Rust 1.78 — **no feature gate**, contrary to older write-ups) so the compiler explains *why* a type does not qualify and what to do about it, instead of emitting a bare `the trait bound X: Y is not satisfied`. Placeholders `{Self}`, `{Trait}`, and the trait's generic parameters interpolate into the message. It changes diagnostics only — trait resolution is unaffected — so it is never a correctness fix, only an ergonomics one; reserve it for traits with non-obvious requirements (sealed traits per API-8, traits with blanket impls that "should" apply, marker traits gating a builder step). The `diagnostic` namespace is best-effort by design: unknown options and malformed values are lint warnings rather than errors, so a typo silently does nothing — confirm the message actually renders. See also `#[diagnostic::do_not_recommend]` (stable since Rust 1.85), which stops the compiler from suggesting an impl that could never apply
- **TRAIT-12.** Put essential functionality in an **inherent** `impl` and have trait impls forward to it, not the other way round. A type whose core methods exist only through a trait forces every caller to discover and `use` that trait before the type does anything — and the compiler's error for the missing import names the method, not the trait to import. Implement `fn download_file(&self, …)` on `HttpClient` itself, then `impl Download for HttpClient { fn download_file(&self, url) { Self::download_file(self, url) } }`. Traits then serve their actual purpose (abstraction over several implementations, TRAIT-9) instead of doubling as the type's only API surface. Exceptions are traits whose whole point is the shared vocabulary (`Display`, `Iterator`, `From`) — those have no inherent counterpart to forward to
- **TRAIT-13.** Public types should be `Send`, and so should the futures they produce, unless the crate deliberately targets a single-threaded runtime and says so. A `!Send` type is not merely restricted itself: held across an `.await` it makes the enclosing future `!Send`, which locks the caller out of `tokio::spawn` on a multi-threaded runtime and out of most runtime-abstracted APIs — and the failure surfaces at the caller's spawn site as a wall of `Rc<…> cannot be sent between threads safely`, far from the field that caused it. Assert it statically at the definition rather than discovering it downstream: `const fn assert_send<T: Send>() {} const _: () = assert_send::<Foo>();` for a named type, and `fn assert_send<T: Send>(_: T) {} _ = assert_send(entry_point());` for the main `async fn` entry points (you need not cover every method). A `!Send` public type is defensible in two cases, both of which have to be deliberate and written down. The first is a type whose use is genuinely instantaneous and never held across an `.await`. The second is a crate written *for* a single-threaded runtime: `tokio::task::spawn_local` on a `LocalSet`, `LocalRuntime`, and `#[tokio::main(flavor = "local")]` all accept `!Send` futures, and thread-per-core designs depend on that. Treat it as a contract rather than an oversight — say so in the crate docs, because a caller on a multi-threaded runtime cannot use the crate at all, and the diagnostic they get will not explain why. The usual cause is an `Rc`, `RefCell`, or raw pointer in a field where an `Arc`/`Mutex` would do; the atomics cost is negligible unless the value is touched in a hot loop (CONC-12). See ASYNC-10 for the trait-method `Send` story and SEC-4 for the `unsafe impl Send` escape hatch, which is not one
- **TRAIT-14.** A bound repeated across a struct, its `impl` blocks, and its signatures should be *named*, not just reformatted. `struct Value<G: FnMut() -> Result<T, Error>, S: Fn(&T) -> Status, T: Display>` restates that bound on every `impl` block and carries a type parameter (`T`) whose only job is to spell the closure's output. Define a trait with an associated type — `trait Getter { type Output: Display; fn get_value(&mut self) -> Result<Self::Output, Error>; }` — and blanket-impl it over the original bound (`impl<F: FnMut() -> Result<T, Error>, T: Display> Getter for F`). The struct becomes `Value<G: Getter, S: Fn(&G::Output) -> Status>`: one parameter shorter, and the bound now says what the parameter is *for*. This goes a step beyond moving bounds into a `where` clause (TRAIT-3), which fixes layout but keeps the repetition and the extra parameter. Two costs: one more item for a reader to find, and the blanket impl is the only thing telling callers that a plain closure still satisfies the trait — say so in the trait's docs (API-14). Not worth it for a bound used once, and see TRAIT-5 on blanket-impl coherence before adding the impl
